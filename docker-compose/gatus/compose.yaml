version: "3.9"
services:
  gatus:
    restart: always
    image: twinproduction/gatus:latest
    container_name: gatus11
    expose:
      - 8080
    volumes:
      - ./config:/config
      - ./data:/data/
    networks: {}
